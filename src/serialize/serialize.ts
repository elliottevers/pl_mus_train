export namespace serialize {
    export class TrainFreezer {
        private env;
        constructor(env) {
            this.env = env;
        }

        public freeze(trainer, filepath) {

        }
    }
}