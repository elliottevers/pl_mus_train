export namespace user_input {
    export class UserInputHandler {
        mode_texture: string;
        mode_control: string;
        constructor(mode_texture, mode_control) {
            this.mode_texture = mode_texture
            this.mode_control = mode_control
        }
    }
}